#pragma once
#include<string>
#include<vector>

class Settings
{
private:
	// last path for processed files
	// files processed so far
	// output file
	// auto load output file
public:
	Settings() = default;
	// Settings()
};